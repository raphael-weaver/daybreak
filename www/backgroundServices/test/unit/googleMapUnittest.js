/*describe("Google Map Service Test", function () {
  var googleMapService;

  beforeEach(function() {
    module('gcalarm');
  });

  beforeEach(inject(function ($injector) {

    $injector.get('window');
    googleMapService = $injector.get('googleMap');
  }));

  it('get Commute time', function () {
    googleMapService.getCommuteTime();
  });
});*/
