/*describe("Google Event Service Test", function () {
  var googleEventService;

  beforeEach(function() {
    module('gcalarm');
  });

  beforeEach(inject(function ($injector) {
    handleAuthClick(null);
    googleEventService = $injector.get('googleEvent');
  }));

  it('run today tasks', function () {
    googleEventService.getTodaysTasks();
  });
});*/
